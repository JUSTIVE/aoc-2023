import { problem,sum } from "../utilities.civet"

processLine := (line:string) =>
  line
    .replace(/[^0-9]/g, "")
    .split ""
  |>(x) => (x.0 + x.-1)

problem
  1,
  (input) =>
    input
    .map processLine
    .map Number
    |>sum
